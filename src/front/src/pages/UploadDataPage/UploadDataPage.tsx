import type { FC } from "react";

import { UploadFilesForm } from "$features/UploadFiles";

export const UploadDataPage: FC = () => {
    return (
        <div>
            <UploadFilesForm />
        </div>
    );
};
